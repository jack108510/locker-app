import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jswenterprises.locker',
  appName: 'Locker',
  webDir: 'out',
  ios: {
    contentInset: 'automatic',
  },
};

export default config;
